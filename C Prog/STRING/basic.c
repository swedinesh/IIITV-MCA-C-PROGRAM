#include<stdio.h>
int main(){
    char str[30];
    printf("enter your name : ");
     scanf("%s",str);
     printf("%s",str);
    // for(int i=0; i<=29; i++)
    // scanf("%c",&str[i]);
    // for(int i=0; i<=29; i++)
    // printf("%c",str[i]);
    return 0;
}